# No secrets needed here — API keys are managed via the UI and stored in PostgreSQL.
# Override these only if you change docker-compose.yml defaults.

DATABASE_URL=postgresql+asyncpg://joh:joh_secret@db:5432/jackofharness
CORS_ORIGINS=http://localhost:3000
